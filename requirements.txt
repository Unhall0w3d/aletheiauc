# Runtime dependency metadata is authoritative in pyproject.toml.
# This legacy convenience file intentionally installs the local project rather
# than maintaining a second, drift-prone dependency list.
.
